import os
import pandas as pd
import numpy as np

DEBUG = bool(os.getenv("DEBUG"))

# this script is used to print a summary of the dataframe
# mean, std, min, max, etc.
# keys, shape, actual size, 

